---
services:
  api:
    image: scottishwidow/api:latest
    restart: always
    build:
      context: express
      dockerfile: Dockerfile
    ports:
      - 7070:3000
    secrets:
      - pg-password
    environment:
      - ENV_VALUE
      - ENV_TOKEN
